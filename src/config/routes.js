const routersConfig = {
  root: '/',
  rootln: '/en',
  profile: '/@:nickname',
  following: '/following',
  upload: '/upload',
  search: '/search',
  live: '/live',
  effect: 'https://effecthouse.tiktok.com/',
};

export default routersConfig;
