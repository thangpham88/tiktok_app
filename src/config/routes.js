const routersConfig = {
  root: '/',
  rootln: '/en',
  profile: '/@:nickname',
  following: '/following',
  upload: '/upload',
  search: '/search',
  effect: 'https://effecthouse.tiktok.com/',
};

export default routersConfig;
