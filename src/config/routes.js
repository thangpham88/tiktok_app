const routersConfig = {
  root: '/',
  profile: '/@:nickname',
  following: '/following',
  upload: '/upload',
  search: '/search',
  effect: 'https://effecthouse.tiktok.com/',
};

export default routersConfig;
